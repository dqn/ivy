{
  "name": "ivy-vn",
  "displayName": "Ivy Visual Novel",
  "description": "Language support for Ivy visual novel scenario files",
  "version": "0.3.0",
  "publisher": "dqn",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets"
  ],
  "activationEvents": [
    "onLanguage:ivy",
    "onLanguage:yaml"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "ivy",
        "aliases": [
          "Ivy Scenario",
          "ivy"
        ],
        "extensions": [
          ".ivy.yaml",
          ".ivy.yml"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "ivy",
        "scopeName": "source.ivy",
        "path": "./syntaxes/ivy.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "ivy",
        "path": "./snippets/ivy.json"
      },
      {
        "language": "yaml",
        "path": "./snippets/ivy.json"
      }
    ],
    "commands": [
      {
        "command": "ivy.preview",
        "title": "Ivy: Open Preview",
        "icon": "$(play)"
      },
      {
        "command": "ivy.validate",
        "title": "Ivy: Validate Scenario"
      },
      {
        "command": "ivy.showInstallGuide",
        "title": "Ivy: Show CLI Installation Guide"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "ivy.preview",
          "when": "resourceLangId == ivy || resourceFilename =~ /\\.yaml$/ || resourceFilename =~ /\\.yml$/",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "ivy.preview",
        "key": "ctrl+shift+p",
        "mac": "cmd+shift+p",
        "when": "resourceLangId == ivy"
      }
    ],
    "configuration": {
      "title": "Ivy",
      "properties": {
        "ivy.previewPort": {
          "type": "number",
          "default": 3000,
          "description": "HTTP port for preview server"
        },
        "ivy.ivyPreviewPath": {
          "type": "string",
          "default": "",
          "description": "Path to ivy-preview binary (leave empty for auto-detection)"
        },
        "ivy.ivyValidatePath": {
          "type": "string",
          "default": "",
          "description": "Path to ivy-validate binary (leave empty for auto-detection)"
        },
        "ivy.lspEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Language Server Protocol features"
        },
        "ivy.lspPath": {
          "type": "string",
          "default": "",
          "description": "Path to ivy-lsp binary (leave empty for auto-detection)"
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "vscode:prepublish": "npm run compile"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dqn/ivy"
  },
  "license": "MIT"
}
